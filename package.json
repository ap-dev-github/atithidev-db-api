{
  "dependencies": {
    "body-parser": "^1.20.3",
    "cors": "^2.8.5",
    "mongoose": "^8.11.0"
  },
  "devDependencies": {
    "serverless": "^3.40.0",
    "serverless-dotenv-plugin": "^6.0.0",
    "typescript": "^5.8.2",
    "typescript-eslint": "^8.26.0"
  },
  "scripts": {
    "lint": "eslint . --ext .ts,.js",
    "test": "jest --coverage",
    "build": "tsc",
    "start": "node dist/index.js",
    "clear-testing-dependencies": "npm uninstall serverless-offline supertest ts-jest @types/jest @types/semver @types/supertest eslint globals jest mongodb-memory-server serverless-prune-plugin",
    "add-prod-dependencies": "npm rm -rf node_modules && npm install --production"
  }
}
